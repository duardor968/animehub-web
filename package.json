{
  "name": "animehub-web",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "packageManager": "pnpm@11.20.0",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=11.20.0"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:web": "pnpm --filter @animehub/web dev",
    "dev:api": "pnpm --filter @animehub/api dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "openapi:generate": "pnpm --filter @animehub/api openapi:generate",
    "openapi:check": "pnpm openapi:generate && git diff --exit-code -- apps/api/openapi.json apps/web/src/lib/api/generated.ts",
    "prisma:generate": "pnpm --filter @animehub/api prisma:generate",
    "prisma:validate": "pnpm --filter @animehub/api prisma:validate"
  },
  "devDependencies": {
    "openapi-typescript": "7.13.0",
    "prettier": "3.9.6"
  }
}
